import React from 'react'
import PageTitle from 'Components/PageTitle'

let ResourcePage = ({resource, model, name}) => {
  return(
    <PageTitle>all {name}</PageTitle>
  )
}

export default ResourcePage
